node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Demo staff credentials (plaintext passwords — NEVER commit)
.local-credentials.txt

# Playwright transient outputs (giữ baselines *-snapshots/, bỏ run artifacts)
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Verification logs/screenshots (local-only, mỗi phase tự sinh)
/.verify
