# SchooIOS Backend — ROADMAP

## Mục tiêu
MVP 7 ngày: auth, cases, workflow, emergency, dashboard, deploy.
Một case đi trọn vòng đời không đứt mạch.

## Workflow
1. Đọc `docs/DATA_MODEL.md` (SSOT) + mục phase trong ROADMAP; spec chi tiết đi theo prompt/PR từng phase (KHÔNG dùng docs/tasks/).
2. Implement từng step, verify bằng `bash scripts/test-all.sh <phase>` (build → tsc → prisma validate → test-<phase>.sh).
3. Sau mỗi step pass: commit "feat: [mô tả ngắn]"
4. Cập nhật checkbox ở đây

## Tiến độ

### Nền móng
- [x] P0: Git branch feature/backend tạo và push
- [ ] P1: Supabase — ✅ dev project + credentials (.env.local) + migrate sạch (pooler aws-1). ⏳ Tách staging/prod để dành P8.
- [x] P2: Prisma schema + nền móng dữ liệu THẬT (13 models: +Building/Location/Class/Enrollment; hardening: caseCode, query-path indexes, immutable triggers, pg_trgm; bootstrap THẬT Lý Tự Trọng — 12 buildings/96 locations/11 cat/3 admin/36 classes/979 students; case thật từ app, seed-dev tách riêng). Xem docs/DATA_MODEL.md + docs/CAMPUS.md

### Core API
- [x] P3: Auth API — login (HS bằng SBD, STAFF/ADMIN bằng email), logout, /me, change-password. **Bắt buộc đổi mật khẩu lần đầu** (cờ `mustChangePassword`, default true cho tài khoản seed). JWT (jose) trong 1 httpOnly cookie; proxy.ts gác /api + ép đổi MK. Hardening: pin alg HS256, timing-safe login, chặn đặt lại MK cũ, primitive `requireUser`. Test: `scripts/test-p3.sh` (20/20). Hoãn: rate-limit→P9, RefreshToken→P5.
- [x] P4: Cases API — POST create + GET list + GET detail. Quyền theo role (Public/Transparent: HS thấy công khai + case của mình; STAFF thấy assigned + NEW/TRIAGED; ADMIN/AUDITOR thấy tất; AUDITOR không tạo). caseCode DB tự sinh; Emergency Hybrid (chỉ `studentFlaggedEmergency`, `isEmergency` luôn false → P7); sensitivity escalate-only từ category; detail `findFirst`+roleWhere → 404 chống enumeration; comment `isInternal` ẩn với HS; `attachments` ẩn `filePath` (Signed URL→P6); audit CREATE qua `recordAudit`; `requireUser` (re-check isActive). Helper `caseWhereForRole`. Test: `scripts/test-p4.sh` (35/35). Hoãn: update/assign→P5.
- [x] P5: Assignment + Workflow — PATCH `assign` + PATCH `status`. State machine 7 trạng thái (`web/src/lib/workflow.ts`: `ALLOWED_TRANSITIONS`/`canTransition`/`statusSideEffects`); ASSIGNED CHỈ sinh qua `assign`; STAFF self-assign NEW/TRIAGED, ADMIN điều phối; reassign GIỮ status (không lùi). Nguyên tử trong 1 `$transaction` (update + statusHistory + auditLog + notification); chống race bằng **optimistic-lock theo `updatedAt`** (version) → 409. Mã lỗi 401/400/403/404/409 + **503**(P2028/P2024→Retry-After, `lib/http-errors.ts`)/500. Audit kèm `clientMeta`; no-op reassign short-circuit; re-read sau commit. Test: `scripts/test-p5.sh` (62/62 — gồm cross-endpoint, interleaving TẤT ĐỊNH qua hook `lib/test-hooks.ts` gated `P5_TEST_HOOKS`, lockcheck, mutation-proven). Hoãn: cột `version` Int (cần migration), CF-6 re-validate authz trong tx, retention audit/history→P9.
- [x] P6: Comments + Notifications — POST/GET comment cho 1 case + GET notifications của tôi (phân trang, `unreadOnly`) + PATCH `read-all`. **Quyền XEM case = quyền BÌNH LUẬN** (qua `caseWhereForRole`, không thấy→404 đồng nhất chống dò); comment NỘI BỘ (`isInternal`) chỉ STAFF/ADMIN tạo, khi notify **LOẠI recipient role=STUDENT** (chống rò rỉ); notify = createdBy+assignedTo trừ commenter (dedupe). Comment+audit(CREATE/"Comment")+notify nguyên tử 1 `$transaction` (`lib/notifications.ts::createNotification(input, tx)`). GET notifications đọc **snapshot nhất quán** bằng `$transaction([...], {isolationLevel: RepeatableRead})` (read-only → không 40001); `read-all` KHÔNG audit (chủ ý). Hardening: `classifyMutationError`→503+Retry-After cho mọi route chạm DB; POST comment trả kèm `author` (parity GET). Test: `scripts/test-p6.sh` (34/34). Hoãn: phân trang comment (thread hiển thị toàn bộ — đúng UX), i18n message→sau.
- [x] P7: Emergency lane + Dashboard aggregate — PATCH `cases/[id]/emergency` (flip `isEmergency` chính thức, KHÁC `studentFlaggedEmergency`; KHÔNG đụng status) + GET `cases/emergency` (lane) + GET `dashboard`. KHÔNG schema migration (`AuditAction.EMERGENCY_FLAG` + `NotificationType.EMERGENCY_CONFIRMED` có sẵn). **Flip nguyên tử** 1 `$transaction` + optimistic-lock `updatedAt`→409; no-op (giá trị không đổi) KHÔNG audit/notify; **chỉ false→true mới notify** (mọi ADMIN active + assignee TRỪ actor); de-escalate chỉ audit. **Lane** bỏ scope STAFF cho case CÔNG KHAI (mọi STAFF/ADMIN/AUDITOR thấy hết) NHƯNG case NHẠY CẢM chỉ ADMIN/AUDITOR + assignee (`OR:[{isSensitive:false},{assignedToId:me}]`); `?activeOnly=true` loại RESOLVED/CLOSED. **Dashboard** count/groupBy THUẦN CODE trong 1 `$transaction` RepeatableRead (Σ byStatus==total sanity); **'stale' thay 'overdue'** (mở ∧ createdAt<now-7d, dueAt deferred); byCategory/byLocation enrich tên (FK Restrict→name không null); avgResolutionHours bỏ. Mã lỗi 401/400/403/404/409/503/500. Hardening sau adversarial-review (workflow 24-agent): vá no-op TOCTOU (re-fetch `findFirst`+`caseWhereForRole`→404), thêm `maybeTestDelay`→test 409 TẤT ĐỊNH. Test: `scripts/test-p7.sh` (66/66 — gồm 401×3, bất biến status/studentFlagged, audit-metadata, ADMIN-actor loại + admin2 nhận, soft-deleted loại, sensitivity gate, optimistic-lock 409). Hoãn: 503-test (mapping dùng chung đã proven P5/P6), lane pagination (emergency hiếm + indexed)→sau.

### Hardening & Deploy
- [x] P8: CI/CD + Deploy runbook — `.github/workflows/ci.yml` gate mọi push/PR: `npm ci` → `prisma generate` (client gitignored) → `eslint --max-warnings 0` → `tsc --noEmit` → `prisma validate` → `next build` (ENV **GIẢ**, offline, KHÔNG DB/deploy). `scripts/test-p8.sh` = CI-mirror hermetic (ẩn `.env`/`.env.local` quanh build) + **secret guard** (grep conn-string thật trong config track) + **drift guard** (ci.yml phải chứa đủ token lệnh). `web/vercel.json` build=`prisma generate && next build` (KHÔNG migrate). `docs/DEPLOY.md`: migrate prod là bước **THỦ CÔNG out-of-band** (`prisma migrate deploy` qua `DIRECT_URL` + guard xác nhận host); staging/prod = 2 Supabase project tách biệt. Commit `22ac76f` (merged main, deployed Vercel — smoke `/api/auth/me`=401). Hoãn: smoke-test runtime/health/env-validation lúc-khởi-động → post-launch.
- [x] P9: Permission audit + Error states + Rate-limit + Load probe — audit KHÔNG đổi quyền (bám CODE-TRUE). `lib/ratelimit.ts` in-memory fixed-window (sweep throttle 1/s + hard-bound) móc vào login (key `ip+identifier`) & change-password (key `user`) **TRƯỚC bcrypt** → **429** + `Retry-After`. Pagination TẤT ĐỊNH: thêm tiebreaker `{id}` cho notifications + comments + statusHistory orderBy (createMany trùng `created_at` → skip/dup nếu thiếu). `scripts/test-p9.sh` (**120/120** qua test-all.sh, 3 vòng sạch giống hệt): ma trận quyền data-driven (5 role × endpoint; 401/403/404/405/409/429), error states, leak-scan PII/passwordHash, dashboard Σ-invariant + **soft-delete guard** (`--live-total` + GET→404), pagination giảm-chặt `(createdAt,id)`, emergency no-op **Δ0 + positive-control**, rate-limit per-user negative-control. `scripts/probe-p9-load.sh` (chạy TAY; prefix `probe-` để KHÔNG lọt glob `test-*`): 50 GET song song 0×500 + write-contention 1×200/9×409. **2 vòng adversarial audit (workflow đa-agent + skeptic mutation-thinking) → 0 production bug**; mọi finding là test-gap/residual đã đóng. **Hợp đồng API đóng băng cho FE merge:** `docs/API.md` (+ Contract Freeze Rules / Frontend MUST-NOT). Commit `ccf0440`. Hoãn: session-invalidation khi đổi MK (cần cột token-version + migration), pin `engines.node="22.x"`, mở rộng secret-guard quét `.ts`/`.sh`, dashboard `_count`/`count` (đã đóng băng — FE đọc đúng key).

### Merge FE↔BE — HOÀN TẤT (M0–M5)
- [x] MERGE Backend ↔ Frontend — FE thật (Next App Router) trên API đóng băng. **M0**: trích UI an toàn (API/proxy/lib byte-unchanged, guard rỗng); layout server-component + AppShell client; +react-hot-toast/zustand/lucide. **M1**: `lib/api.ts`+`api-types.ts` + 3 read endpoint (categories/locations/audit) + API.md. **M2**: auth thật (login/me/logout/change-password) + proxy gác CẢ trang LẪN /api, chống redirect-loop. **M3**: cases/report e2e (create→caseCode, list/detail, comment, status/assign/emergency; STUDENT read-only; case-display.ts 1-chiều). **M4**: notifications/audit/dashboard nối API (badge server-truth, dashboard ADMIN/AUDITOR). **M5**: parity quyền + error states + e2e (best-effort) + `scripts/test-integration.sh` (shared-server).
  - Test: `test-m1`=21, `m2`=23, `m3`=14, `m4`=12 (FAIL=0); backend `test-p3..p9` GIỮ xanh. Contract-guard rỗng (không isConfidential/SOS-/localStorage/persist/status-VN ở tầng dữ liệu).
  - **Audit đối kháng đa-agent** (8 reviewer + skeptic-verify): 8.1→**9.0/10**, 0 auth-bypass / 0 PII-leak / 0 contract-drift; proxy giữ /api gating byte-for-byte. Vá A–J: 401-loop (cookie-clear trước redirect), unread badge server-truth, list server-side pagination, Retry-After header (`ApiError.retryAfter`), POST cases enrich+503, proxy dual-matcher, read-error toasts, guard quét `lib/`.
  - Commits: `4f03433`(M0) `040fa78`(M1) `93afe06`(M2) `3e7cf55`(M3) `e60e8aa`(M4) `f2ba94f`(M5) + hardening.
  - **Deploy:** KHÔNG đổi schema (chỉ thêm 3 read route + FE) → DB prod đã migrate sẵn (P8/P9). Triển khai = merge `feature/integration`→`main`, Vercel auto-build. KHÔNG cần `migrate deploy` cho lần này.

### FE Immersive — Lớp vận hành (Đợt 1)
- [x] F6 ĐỢT 1 (FE-only, branch `feature/immersive`, KHÔNG đụng backend/schema) — bồi lớp "trung tâm điều hành" trên API đóng băng. **A** `/cases` Duyệt tất cả (ADMIN/AUDITOR; lọc status/khẩn SERVER-DRIVEN qua `useCaseList`; drill-down từ shell `await searchParams`; ngoại lệ client-filter DUY NHẤT = category facet lát-hiện-tại). **B** Dashboard 2 chart thuần SVG/CSS (segmented status bar + category bars; KHÔNG donut/canvas/lib) ADDITIVE vào `AdminHome` + drill-down `→/cases?status` / `?category&cat`. **C** `/desk` My Desk (STAFF; 1 fetch limit=100 → 6 bucket client-facet, KHÔNG nới quyền). **D** Aging badge (`lib/case-aging.ts` theo `createdAt` CLIENT, KHÔNG SLA/dueAt server; tier fresh/aging/stale khớp dashboard; nhúng AllCases/Desk/Report). **E** `/feed` Student read-only (KHÔNG `mine` → công khai ∪ của-mình; `lib/relative-time.ts`). Nav thêm 3 mục theo role. GATE: tsc 0 · eslint 0 · build pass (server bundle 0 three/lenis/gsap) · contract-guard grep sạch · e2e `f6-ops.spec.ts` 9/9 · backend `test-all.sh p9` **120/0** (diff FE-only ⇒ backend byte-identical). Tag `warm-f6-ops`. KHÔNG `migrate deploy`. Hoãn → **Đợt 2**: attachments (Storage bucket + signed-upload + camera). Defer: assignment cockpit · GET /api/users · search · sửa/xoá report · SLA/dueAt thật · time-series chart.
  - **F6 Đợt 1.5 — Assignment Cockpit → tag `warm-f6-cockpit`** (branch `feature/immersive`). BE: `GET /api/users` (ADMIN-only, 0 PII `{id,name,role}`, test-users 19/0). FE: ADMIN picker "Giao cho…" + workload tally + §3c "Mở lại" label + §3b Bulk mode AllCasesView (ADMIN toggle, ISOLATION TẮT=Đợt-1-identical, concurrency-4, 409 reload+retry). GATE: tsc/eslint/build 0 · test-users 19/0 · p9 120/0. **FE fully ready for merge → main (awaiting owner approval).**
  - **QA đối kháng (reverse-thinking) → tag `warm-f6-ops-qa`** (rollback point `pre-qa-f6`). Sweep R1→fix R2→harden R3: vá 2 FE-bug — (B1) `DeskView` chia bucket khi session CHƯA resolve (me="") → loé rỗng → gate thêm `sessionLoading`; (B2) `DashboardSkeleton` thiếu khối "Xu hướng" → layout-shift lúc load → thêm skeleton block. Mọi giả thuyết §2 còn lại = hành-vi-đúng (server-scope/404-đồng-nhất/per-spec), KHÔNG sửa. Bằng chứng: aging-boundary 8/8 (days 0/1→fresh·2/6→aging·7→stale·RESOLVED/CLOSED→0 badge), e2e f6-ops **15/15** (+matrix anon→/login, STAFF /desk 0-call /dashboard), screenshot 1280+390 mọi surface on-brand (server-scope CHỨNG MINH: STUDENT /feed thấy ít hơn ADMIN, KHÔNG lộ case nhạy cảm), backend p9 **120/0**, tsc/eslint/build 0, contract-guard 0.

### Updates sau GO-LIVE (branch `feature/update-b-broadcast`)
- [x] **Update B — BGH phát thông tin (Feed Broadcast)** (branch `feature/update-b-broadcast` từ `main` @ `e5eb89a`, tag `pre-update-b`/`update-b-broadcast`). ĐỘC LẬP với Update A. **MIGRATION THUẦN ADDITIVE** (5 bảng mới, `prisma migrate deploy` out-of-band trước deploy). **Post** (model `Post`, POST/GET `/api/posts`, POST/DELETE `/api/posts/[id]/upvote` upvote-only idempotent `@@unique([userId,postId])` KHÔNG cột value, DELETE `/api/posts/[id]` ADMIN soft-delete; KHÔNG comment). **Poll** (model `Poll`+`PollOption`+`PollVote`, POST/GET `/api/polls`, POST `/api/polls/[id]/vote` 1-phiếu/user đổi=upsert `@@unique([userId,pollId])`, optionId-thuộc-poll+poll-đóng guard, DELETE ADMIN; count/percent chia-0 an toàn, isClosed). Tạo/xoá CHỈ ADMIN; đọc mọi role; tương tác mọi role TRỪ AUDITOR (403). KHÔNG notify (broadcast pull-based, tránh enum mới). Audit CREATE/UPDATE/DELETE entityType Post/Poll/PostVote/PollVote (KHÔNG enum mới). author chỉ `{id,name,role}` (0 PII). `lib/broadcast.ts` enrich (1 groupBy+1 findMany/trang → KHÔNG N+1). FE: `BroadcastLane` (lane "Thông báo & Bình chọn" THÊM trên cùng `/feed`, stream case Đợt-1 byte-identical) · `PostCard` (optimistic like+rollback) · `PollCard` (% bars, đổi lựa chọn, my-choice) · Create modals ADMIN · `useBroadcast` hooks · nav +Bảng tin cho STAFF/ADMIN/AUDITOR. GATE: tsc 0 · eslint 0 · build 0 · `scripts/test-posts-polls.sh` **93/0** · regression p9 **120/0** · diff-guard 23 file in-scope · contract-guard 0. Commits `d30bd90`(schema) `0bd5ff7`(BE) `0ec2dc0`(test) `34c63f8`(FE). Tag `update-b-broadcast`. **Prod migration runbook**: `DIRECT_URL` → guard host → `npx prisma migrate deploy` → smoke `/api/posts` + `/api/polls`.
